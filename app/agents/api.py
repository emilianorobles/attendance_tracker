"""
API Routes for Agent Management.
Placeholder module - agent functionality is in roster routes.
"""
from fastapi import APIRouter

router = APIRouter(prefix="/api/agents", tags=["agents"])

# Agent routes are handled in roster.py
# This router exists for compatibility
